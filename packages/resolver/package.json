{
  "name": "@design-parity/resolver",
  "version": "0.0.0",
  "description": "Correspondence resolver: decide which design reference (source, ref) matches a changed code component — Code Connect, then design-map.json, then name convention.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@design-parity/core": "0.0.0"
  }
}
