{
  "name": "@design-parity/policy",
  "version": "0.1.0",
  "description": "Parity direction policy: the committed .design-parity.json config (schema, loader, validator CLI) and the deterministic auto -> design-led/code-led resolver.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./schema/parity-config.schema.json": "./schema/parity-config.schema.json"
  },
  "files": [
    "dist",
    "schema"
  ],
  "bin": {
    "design-parity-validate-config": "./dist/cli/validate-config.js"
  },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@design-parity/core": "^0.1.0",
    "ajv": "^8.17.1"
  }
}
