{
  "name": "design-parity",
  "version": "0.0.0",
  "private": true,
  "description": "A tool-neutral bot that proves a UI pull request is at parity with its intended design.",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/adapters/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @design-parity/core && npm run build --workspaces --if-present",
    "typecheck": "npm run build",
    "test": "vitest run",
    "validate": "npm run build --workspace @design-parity/core && node packages/core/dist/cli/validate-design-map.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=22"
  }
}
