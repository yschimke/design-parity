{
  "name": "design-parity-monorepo",
  "version": "0.1.16",
  "private": true,
  "description": "Monorepo root for design-parity. The publishable CLI is the top-level `design-parity` package (packages/cli); the building blocks are the `@design-parity/*` scope.",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/adapters/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @design-parity/core && npm run build --workspaces --if-present",
    "typecheck": "npm run build",
    "test": "vitest run",
    "set-version": "node scripts/set-version.mjs",
    "validate": "npm run build --workspace @design-parity/core && node packages/core/dist/cli/validate-design-map.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=22"
  }
}
